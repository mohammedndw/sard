You are working in a Replit workspace. Redevelop my existing website (currently built with plain HTML + CSS) into a modern React frontend with a Firebase backend using Cloud Functions.

GOAL
- Rebuild the website using React (frontend) while keeping the same UI/branding from the current HTML/CSS as much as possible.
- Create a Firebase Cloud Functions backend for any dynamic features (forms, API endpoints, etc.).
- Everything should run locally inside Replit with clear scripts and documentation.

PROJECT STRUCTURE (Monorepo)
Create a single repo with two folders:
1) /frontend  (React app)
2) /functions (Firebase Cloud Functions - Node.js)

FRONTEND REQUIREMENTS
- Use React with Vite.
- Convert each existing HTML page/section into React components:
  - Layout: Header, Footer, Navigation
  - Pages: Home, About, Services, Contact (and any other pages found)
  - Reusable components: Buttons, Cards, Sections
- Reuse the existing CSS:
  - Move the current CSS into /frontend/src/styles (or similar)
  - Ensure class names match or map them cleanly
- Use React Router for navigation between pages.
- Add a simple config file for environment variables (VITE_*).
- Make the UI responsive and keep the original look/spacing/colors.

BACKEND REQUIREMENTS (Firebase Cloud Functions)
- Use Firebase Functions (Node.js + Express).
- Create an API base path: /api
- Add example endpoints:
  - GET /api/health  -> returns { status: "ok" }
  - POST /api/contact -> accepts { name, email, message } and:
      - validates input
      - logs the message (and optionally stores it in Firestore if configured)
      - returns a success JSON response
- Add basic CORS support to allow calls from the React frontend.

INTEGRATION
- From the React frontend, connect the Contact form to POST /api/contact using fetch/axios.
- Show loading state, success message, and error message in the UI.

REPLIT SETUP
- Provide package.json scripts so I can run both frontend and functions easily.
- Add a root README.md with:
  - how to install dependencies
  - how to run frontend
  - how to run functions emulator / local server
  - how to set environment variables in Replit

QUALITY / SAFETY
- Keep code clean and modular.
- Validate inputs on backend.
- Do not hardcode secrets. Use environment variables.

IMPORTANT
- Start by scanning the current project files to identify existing HTML pages and CSS files.
- Then implement the React version to match the current structure and design.
- After that, implement Firebase Cloud Functions and wire up the Contact form end-to-end.
